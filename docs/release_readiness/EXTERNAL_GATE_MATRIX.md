# External Gate Matrix (PCA-18/19)

**GENERATED FILE -- do not edit by hand.** Regenerate with
`node tooling/release/GenerateExternalGateMatrixMd.mjs` after any change to
[`external_gate_matrix.json`](./external_gate_matrix.json), which remains the
only authority `tooling/release/Invoke-ReleaseGateCheck.ps1` reads. The CI
release-control job runs this generator with `--check` and fails if this
file is stale. (Until 2026-09-08 this page listed only the original seven
gates while the JSON held far more; it is now derived.)

These are gates this repository-editing lane cannot close by writing code
or docs -- each requires a real human decision, real hardware, a real
provider, or a real external review outside the source tree.

Register state: **39 gates**, last updated 2026-09-08T00:00:00.0000000Z.
Closed: **0**. Open (BLOCKED or EXTERNAL): **39**.

Legend per release target: **HARD** = the gate is in the target's `releaseScope` and
blocks that target's base release while open; *cond* = in `conditionalReleaseScope`
(a real, feature-scoped dependency surfaced separately, never a base-release blocker);
blank = the gate does not apply to that target.

| Gate ID | Status | PUBLIC_A | AUTH_B | PARENT_C | ANDROID_D | IOS_FUTURE | BILLING_FUTURE | Owner | What it covers |
|---|---|---|---|---|---|---|---|---|---|
| `CRYPTO_SECURITY_REVIEW` | BLOCKED |  |  | **HARD** | **HARD** | **HARD** |  | External security reviewer (not yet assigned) | Human security review of the production crypto suite (DeviceSignatureVerifier, EnvelopeSignatureVerifier) has not occurred. See docs/security/production-crypto-review/. Backend currently wires RejectingDeviceSignatureVerifier / RejectingEnvelopeSignatureVerifier which fail closed by design. |
| `ANDROID_REAL_DEVICE_UAT` | BLOCKED |  |  |  | **HARD** |  |  | Android device owner / QA (not yet assigned) | No real Android hardware has run the UAT_TEST_PLAN.md case catalogue. This lane has JVM unit-test evidence only (see evidence pack), which does not satisfy this gate. |
| `IOS_MAC_XCODE` | EXTERNAL |  |  |  |  | **HARD** |  | iOS build owner (not yet assigned) | No macOS/Xcode build environment is available in this environment. iOS build/test/signing cannot be performed here. |
| `IOS_FAMILY_CONTROLS_ENTITLEMENT` | EXTERNAL |  |  |  |  | **HARD** |  | Apple Developer Program account holder (not yet assigned) | Apple Family Controls entitlement has not been granted/confirmed. Required before any real iOS parental-control capability can ship. |
| `IOS_PHYSICAL_DEVICE` | EXTERNAL |  |  |  |  | **HARD** |  | iOS device owner (not yet assigned) | No physical iPhone/iPad is available for UAT or Family Controls testing (simulator cannot exercise Family Controls/Screen Time APIs). |
| `YOUTUBE_MODE_B_POLICY_REVIEW` | BLOCKED |  |  |  | *cond* | *cond* |  | Compliance/legal owner (not yet assigned) | YouTube Mode B (PCA-controlled playback) policy/compliance review with YouTube API Terms of Service has not been completed. Backend feature flag defaults disabled and unreviewed (see ModeBFeatureFlagStore tests). |
| `CLOUD_AI_OWNER_DECISION` | BLOCKED |  |  |  | *cond* | *cond* |  | Product/privacy owner (not yet assigned) | Owner decision on cloud-based AI classifier usage (vs. on-device only) has not been made. On-device model lifecycle/package verification gate exists but cloud-provider decision is unresolved. |
| `PRODUCTION_CRYPTO_SECURITY_REVIEW` | BLOCKED |  |  | **HARD** | **HARD** | **HARD** |  | Security and release owner (not yet assigned) | Production crypto verifier activation requires the independent security review recorded by the release gate. |
| `CRYPTO_ACTIVATION` | BLOCKED |  |  | **HARD** | **HARD** | **HARD** |  | Security and release owner (not yet assigned) | Signed runtime acceptance and delivery remain fail-closed until the reviewed production crypto suite is activated. |
| `ANDROID_APP_LINK_ASSETLINKS_HOSTING` | EXTERNAL |  |  |  | **HARD** |  |  | Android/domain owner (not yet assigned) | Android App Link assetlinks hosting and ownership verification require the production domain owner. |
| `ANDROID_PHYSICAL_INSTALL_CONTINUATION` | EXTERNAL |  |  |  | **HARD** |  |  | Android device owner / QA (not yet assigned) | Physical Android install and continuation testing require a real device and production distribution path. |
| `SUPPORTED_CHARGE_CURRENCIES` | EXTERNAL |  |  |  |  |  | **HARD** | Commercial/finance owner (not yet assigned) | Supported charge-currency approval is a commercial and finance owner decision, not a local source-only claim. |
| `SUPPORTED_SETTLEMENT_CURRENCIES` | EXTERNAL |  |  |  |  |  | **HARD** | Finance/settlement owner (not yet assigned) | Supported settlement-currency approval requires the accountable finance and settlement owner. |
| `PAYMENT_PROVIDER_SELECTION` | EXTERNAL |  |  |  |  |  | **HARD** | Commercial/finance owner (not yet assigned) | A production payment provider has not been selected and approved for this repository lane. |
| `MERCHANT_ACCOUNT_APPROVAL` | EXTERNAL |  |  |  |  |  | **HARD** | Commercial/finance owner (not yet assigned) | Merchant-account onboarding and approval require an external provider and accountable finance owner. |
| `SETTLEMENT_BANK_CONFIGURATION` | EXTERNAL |  |  |  |  |  | **HARD** | Finance/settlement owner (not yet assigned) | Settlement bank configuration requires an external finance owner and provider-approved account. |
| `DEVICE_OWNER_REAL_DEVICE_AUTHORIZATION` | EXTERNAL |  |  |  | **HARD** |  |  | Android device owner / QA (not yet assigned) | Android device-owner authority and real-device authorization cannot be established by repository source alone. |
| `PLATFORM_ADMIN_ALERT_DELIVERY` | EXTERNAL |  | *cond* | *cond* | *cond* |  |  | Platform operations owner (not yet assigned) | Durable Platform Administration security-alert rows are generated locally; configuring and independently verifying delivery to other APP_OWNER accounts through an approved email/SMS/paging channel requires an external operations provider. |
| `REAL_DEVICE_TELEPHONY_UAT` | EXTERNAL |  |  |  | **HARD** |  |  | Android device owner / QA (not yet assigned) | Real-device telephony behavior requires hardware, carrier, and controlled UAT evidence. |
| `REAL_DEVICE_TELEPHONY_SMS_UAT` | EXTERNAL |  |  |  | **HARD** |  |  | Android device owner / QA (not yet assigned) | Real-device SMS delivery and locked-state behavior require hardware, carrier, and controlled UAT evidence. |
| `PRODUCTION_CRYPTO_SUITE` | BLOCKED |  |  | **HARD** | **HARD** | **HARD** |  | External security reviewer (not yet assigned) | Aggregate external dependency for requirements (retention, tamper-monitoring, envelope sync, identity) whose remaining gap is the production cryptographic suite (DeviceSignatureVerifier / EnvelopeSignatureVerifier) not yet being reviewed and activated. Backend wires RejectingDeviceSignatureVerifier / RejectingEnvelopeSignatureVerifier, which fail closed by design until the review completes. Tracked as a distinct register token from CRYPTO_SECURITY_REVIEW / PRODUCTION_CRYPTO_SECURITY_REVIEW / CRYPTO_ACTIVATION because the local source half of each row it covers is independently confirmed complete. Does NOT block AUTH_B: verifyEmail marks the account VERIFIED and issues a session even when family genesis fails, so identity/login work without it. |
| `CAMERA_REAL_DEVICE_VALIDATION` | EXTERNAL |  |  |  | **HARD** |  |  | Android device owner / QA (not yet assigned) | Physical-hardware confirmation that on-device camera-based proximity/face-geometry classification (FaceProximityClassifier, AndroidFaceGeometryDetector, CameraProximitySource) behaves correctly across real OEM camera stacks. Source structurally can only emit NEAR/FAR/UNKNOWN with no persistence/network path (guarded by CameraPrivacyStaticScanTest.kt); no Android SDK/device is available in this environment to run the confirmation. |
| `YOUTUBE_PLATFORM_API_PARTNERSHIP` | EXTERNAL |  |  |  | *cond* | *cond* |  | Commercial/partnerships owner (not yet assigned) | A compliant Restricted Mode / platform-API signal integration for YouTube requires a real YouTube/platform API partnership and terms review that has not been established. defaultYouTubeSafeContentCapability() correctly returns UNSUPPORTED/UNAVAILABLE by construction in the absence of that partnership, guarded by a regression test (SafeContentCapability.test.mjs) against ever introducing scraping/watch-history-monitoring/TLS-interception. |
| `PENDING_OWNER_DECISION` | BLOCKED |  |  |  | **HARD** |  |  | Product owner (not yet assigned) | Generic canonical token for requirements blocked on a genuine, still-open product/architecture owner decision rather than a repository-solvable gap (sourceSolvableClass SOURCE_COMPLETE_OWNER_DECISION_GATE). Specific decisions tracked under this gate include PCA-DEC-002/014/015 (Android provisioning path: QR-code PROVISION_MANAGED_DEVICE vs. ADB pilot-only) -- see each requirement's own notes field for its specific decision citation; this canonical token itself must never be edited to imply any decision has been made. |
| `REQUIRES_ENTITLEMENT` | EXTERNAL |  |  |  |  | **HARD** |  | Apple Developer Program account holder (not yet assigned) | Requires the Apple Family Controls entitlement grant/confirmation before ChildAuthorizationCenter.swift's authorization flow can be exercised against production Apple infrastructure. Related to, but registered separately from, IOS_FAMILY_CONTROLS_ENTITLEMENT. |
| `DEPLOYED_LOG_METRICS_PIPELINE_CONFIG` | EXTERNAL |  | *cond* | *cond* | *cond* |  |  | Platform operations owner (not yet assigned) | Deployed log/metrics pipeline configuration (reverse proxy / container log shipping) for the relay's routing-metadata-only logging surface. Source proof is strong (type-level exclusion of plaintext fields, Fastify logger disabled, noPayloadLoggingHttp.test.mjs regression, zero stray console/log call sites); the remaining gap is infra-level deployment configuration not present in this repository beyond a plain Dockerfile. |
| `DEPLOYED_TLS_TERMINATION_CONFIG` | EXTERNAL | **HARD** | **HARD** | **HARD** | **HARD** | **HARD** | **HARD** | Platform operations owner (not yet assigned) | Deployed production TLS-termination configuration (reverse proxy / load balancer) is an infrastructure concern outside this repository. Confirmed via direct repo-wide search that no TLS termination is implemented or claimed in-repo, so this requirement is correctly NOT_APPLICABLE to source with the deployment step tracked as an external gate. Any HTTPS publication, on any release target, needs TLS termination configured -- the one gate here that is genuinely scoped to every target including PUBLIC_A. |
| `TELEMETRY_ACTIVATION_OWNER_SIGNOFF` | BLOCKED |  |  |  |  |  |  | Product/privacy owner (not yet assigned) | Formal product/privacy-owner sign-off required before ever activating optional telemetry. Consent module (backend/src/telemetry/consent.ts) defaults off, has an explicit distinct grant/revoke, enforces an allowlisted aggregate-only event shape, and noTelemetryEndpoint.test.mjs proves zero ingestion routes exist anywhere in the product; building ingestion infrastructure ahead of sign-off would violate PCA-NFR-014/PRIV-002 rather than satisfy it. This gate governs ACTIVATING telemetry, not any release capability -- it can never legitimately close before a release and must never be allowed to block one either, so its releaseScope is intentionally empty (see docs/supervision/PCA_FABLE_EXTERNAL_GATE_RELEASE_SCOPE.csv: 'a permanent false blocker' prior to this fix). |
| `OFFLINE_DEVICE_INTERRUPTION_RECOVERY_VALIDATION` | EXTERNAL |  |  |  | **HARD** |  |  | Android device owner / QA (not yet assigned) | Physical-device validation that schedule/retention runtime correctly recovers state across real offline periods and process interruptions (RetentionEngine, RetentionMaintenanceCycle, ScheduleRuntime reboot/offline handling). The repository-solvable portion (a real RetentionEngine production caller) is closed and independently verified; the remaining gap is device-lab confirmation, which this environment lacks. |
| `PCA_DEC_009_DISCLOSURE_TEXT` | BLOCKED |  |  |  | **HARD** |  |  | Product owner (not yet assigned) | Small, distinct pending-owner-decision sub-item (decision PCA-DEC-009) on final disclosure text for the battery-budget policy requirement (PCA-NFR-034), kept as its own gate token rather than folded into the generic PENDING_OWNER_DECISION bucket or the ANDROID_REAL_DEVICE_UAT/IOS_PHYSICAL_DEVICE gates it accompanies on that row, so the decision is not hidden behind an unrelated gate. |
| `PRODUCER_CATALOGUE_AUDIT_SIGNOFF` | BLOCKED |  |  | **HARD** |  |  |  | Compliance/privacy owner (not yet assigned) | Formal review/sign-off confirming the child-activity producer disclosure catalogue (parent-web Transparency.tsx) is complete. Every enumerable producer currently traces to a disclosed category and no concrete undisclosed producer was found, but completeness of all future platform/backend producers cannot be proven by source alone. |
| `OEM_DEVICE_DIVERSITY_VALIDATION` | EXTERNAL |  |  |  | **HARD** |  |  | Android device owner / QA (not yet assigned) | Real-device validation that OEM/AOSP dialer and communication-surface resolution (AndroidCommunicationSurfaceResolver, TelecomManager.getSystemDialerPackage()) behaves correctly across the diversity of real OEM Android devices. Hardcoded OEM assumptions were removed and unresolved cases now honestly report unavailable rather than being silently protected; the remaining need is a device lab this environment lacks. |
| `OBSERVABILITY_PIPELINE` | EXTERNAL |  |  | *cond* |  |  |  | Platform operations owner (not yet assigned) | Deployed observability/monitoring pipeline for the Platform Administration dashboard read-model and settlement-metrics routes is an infra-level dependency outside this repository; the in-repo dashboard read-model and its authorization-boundary tests are independently validated. |
| `OWNER_VISUAL_UAT` | BLOCKED | **HARD** |  |  |  |  |  | Product/design owner (not yet assigned) | Owner visual UAT of the Public site (public-web) is still IN_PROGRESS. This is a genuine Release A (PUBLIC_A) blocker distinct from the automated/technical gates: a human owner must actually look at the published public site and sign off before Release A can be considered ready. Was previously absent from this register entirely, so no tooling could ever surface it. See docs/supervision/PCA_FABLE_OPEN_ACTION_REGISTER.csv FABLE-A063 and docs/supervision/PCA_FABLE_EXTERNAL_GATE_RELEASE_SCOPE.csv. |
| `PUBLIC_REPLY_IDENTITY` | BLOCKED | **HARD** | *cond* |  |  |  |  | Platform operations owner (not yet assigned) | Inbound delivery to the four public addresses (contact, security-disclosure, etc.) has been verified, but public Send-As / reply identity remains NOT_READY -- the product cannot yet reply to the public from its own identity. A published contact/security-disclosure surface that cannot reply from its own identity is a genuine Release A (PUBLIC_A) hard blocker. It also touches AUTH_B's shared email sending / domain-authentication (SPF/DKIM/DMARC) path, but per docs/supervision/PCA_FABLE_EXTERNAL_GATE_RELEASE_SCOPE.csv that is a PARTIAL/conditional dependency for AUTH_B, not a hard base-release blocker -- AUTH_B's own hard sender/domain-authentication requirement is tracked under PRODUCTION_EMAIL_DELIVERY (do not double-count it here as a hard AUTH_B blocker). Do not expose the private forwarding mailbox in any artifact. Was previously absent from this register entirely. See docs/supervision/PCA_FABLE_OPEN_ACTION_REGISTER.csv FABLE-A064 and docs/release_readiness/reports (RELEASE_A_CONTACT_CHANNEL_VERIFICATION.md referenced there). |
| `PRODUCTION_EMAIL_DELIVERY` | EXTERNAL |  | **HARD** | **HARD** | *cond* | *cond* |  | Email delivery provider owner (not yet assigned) | PCA-DW-W2/W2-R1 built a complete production-grade email architecture in backend/src/email (EmailService, an encrypted-at-rest durable outbox, bounded retry/dead-letter, SMTP + Microsoft Graph provider adapters, keyed idempotency, provider timeouts, TLS enforcement, redacted error metadata) -- this gate is NOT about missing code. With no PCA_EMAIL_PROVIDER configured, the architecture correctly fails closed to a RejectingEmailProviderAdapter (never a silent fake-success), so markVerified still has no real production email path and no parent can log in without one; this remains the AUTH_B blocker and must not be filed under PAYMENT_PROVIDER_SELECTION. Requires: an approved real provider; production credentials configured; verification-email delivery proven externally; reset-email delivery proven externally; sender identity and domain authentication (SPF/DKIM/DMARC) verified; and no test-sandbox adapter active in production. PUBLIC_A does not need this (no account system on the public site). ANDROID_D/IOS_FUTURE are a PARTIAL/conditional dependency only (where enrollment invitations travel by email) -- NOT a hard blocker of their base release, per docs/supervision/PCA_FABLE_EXTERNAL_GATE_RELEASE_SCOPE.csv. See docs/supervision/PCA_FABLE_OPEN_ACTION_REGISTER.csv FABLE-A006/FABLE-A061 and docs/supervision/PCA_DYNAMIC_WORKFLOW_W2_CLOSURE.md for the architecture's own evidence. |
| `PAYMENT_PRODUCTION_CERTIFICATION` | EXTERNAL |  |  |  |  |  | **HARD** | Commercial/finance owner (not yet assigned) | Real-money go-live certification for the production payment provider, distinct from mere provider *selection* (PAYMENT_PROVIDER_SELECTION) or merchant onboarding (MERCHANT_ACCOUNT_APPROVAL) -- this gate covers the final certification/approval to activate live (non-sandbox) payment processing. Specified in multiple release-readiness documents but was absent from this JSON register entirely, so the gate governing real-money go-live could never block a release. Governs BILLING_FUTURE only. See docs/supervision/PCA_FABLE_OPEN_ACTION_REGISTER.csv FABLE-A026. |
| `ANDROID_RELEASE_SIGNING_CONFIG` | EXTERNAL |  |  |  | **HARD** |  |  | Android/release owner (not yet assigned) | android/app/build.gradle.kts declares only a debug buildType: there is no release buildType, no signingConfig, no keystore, minify/ProGuard are wired to nothing, and CI has only ever built assembleDebug -- so no installable, signed release artifact for ANDROID_D has ever existed. A keystore and Play App Signing decision are owner-held and must never be fabricated in-repo. Recommended and registered by the 2026-09-07 external-readiness report (O-1); added to this register by the 2026-09-08 final assessment so tooling can surface it. |
| `PRODUCTION_EMAIL_CREDENTIAL_ROTATION` | BLOCKED |  | **HARD** | **HARD** | *cond* | *cond* |  | Email delivery provider owner / platform operations owner (not yet assigned) | The owner reported on 2026-09-08 that the SMTP relay credential configured for the backend App Service (Key Vault secret PCA-SMTP-PASSWORD, created 2026-09-07) is COMPROMISED. Until the owner has revoked it at the email provider, issued a replacement, and stored the replacement in Key Vault, no deployment of AUTH_B/PARENT_C may proceed -- a compromised outbound-mail credential lets a third party send verification/reset mail as PCA. The repository cannot verify rotation; this gate is closed only by the accountable owner recording the rotation evidence here. Distinct from PRODUCTION_EMAIL_DELIVERY (which covers provider selection and proven delivery), so a proven delivery path cannot mask an unrotated credential. |

## Per-target hard blockers (derived)

- **PUBLIC_A** -- 3 hard: `DEPLOYED_TLS_TERMINATION_CONFIG`, `OWNER_VISUAL_UAT`, `PUBLIC_REPLY_IDENTITY`.
  Plus the two source-derived signals the gate script evaluates before this register: `PRODUCTION_CRYPTO_SUITE` (from `backend/src/main.ts`; scoped to PARENT_C/ANDROID_D/IOS_FUTURE) and `REAL_UAT` (from `uat_execution_log.json`; scoped per target, and structurally unsatisfiable for PUBLIC_A/IOS_FUTURE/BILLING_FUTURE until the owner decides how those targets satisfy it).
- **AUTH_B** -- 3 hard: `DEPLOYED_TLS_TERMINATION_CONFIG`, `PRODUCTION_EMAIL_DELIVERY`, `PRODUCTION_EMAIL_CREDENTIAL_ROTATION`; 3 conditional: `PLATFORM_ADMIN_ALERT_DELIVERY`, `DEPLOYED_LOG_METRICS_PIPELINE_CONFIG`, `PUBLIC_REPLY_IDENTITY`.
  Plus the two source-derived signals the gate script evaluates before this register: `PRODUCTION_CRYPTO_SUITE` (from `backend/src/main.ts`; scoped to PARENT_C/ANDROID_D/IOS_FUTURE) and `REAL_UAT` (from `uat_execution_log.json`; scoped per target, and structurally unsatisfiable for PUBLIC_A/IOS_FUTURE/BILLING_FUTURE until the owner decides how those targets satisfy it).
- **PARENT_C** -- 8 hard: `CRYPTO_SECURITY_REVIEW`, `PRODUCTION_CRYPTO_SECURITY_REVIEW`, `CRYPTO_ACTIVATION`, `PRODUCTION_CRYPTO_SUITE`, `DEPLOYED_TLS_TERMINATION_CONFIG`, `PRODUCER_CATALOGUE_AUDIT_SIGNOFF`, `PRODUCTION_EMAIL_DELIVERY`, `PRODUCTION_EMAIL_CREDENTIAL_ROTATION`; 3 conditional: `PLATFORM_ADMIN_ALERT_DELIVERY`, `DEPLOYED_LOG_METRICS_PIPELINE_CONFIG`, `OBSERVABILITY_PIPELINE`.
  Plus the two source-derived signals the gate script evaluates before this register: `PRODUCTION_CRYPTO_SUITE` (from `backend/src/main.ts`; scoped to PARENT_C/ANDROID_D/IOS_FUTURE) and `REAL_UAT` (from `uat_execution_log.json`; scoped per target, and structurally unsatisfiable for PUBLIC_A/IOS_FUTURE/BILLING_FUTURE until the owner decides how those targets satisfy it).
- **ANDROID_D** -- 17 hard: `CRYPTO_SECURITY_REVIEW`, `ANDROID_REAL_DEVICE_UAT`, `PRODUCTION_CRYPTO_SECURITY_REVIEW`, `CRYPTO_ACTIVATION`, `ANDROID_APP_LINK_ASSETLINKS_HOSTING`, `ANDROID_PHYSICAL_INSTALL_CONTINUATION`, `DEVICE_OWNER_REAL_DEVICE_AUTHORIZATION`, `REAL_DEVICE_TELEPHONY_UAT`, `REAL_DEVICE_TELEPHONY_SMS_UAT`, `PRODUCTION_CRYPTO_SUITE`, `CAMERA_REAL_DEVICE_VALIDATION`, `PENDING_OWNER_DECISION`, `DEPLOYED_TLS_TERMINATION_CONFIG`, `OFFLINE_DEVICE_INTERRUPTION_RECOVERY_VALIDATION`, `PCA_DEC_009_DISCLOSURE_TEXT`, `OEM_DEVICE_DIVERSITY_VALIDATION`, `ANDROID_RELEASE_SIGNING_CONFIG`; 7 conditional: `YOUTUBE_MODE_B_POLICY_REVIEW`, `CLOUD_AI_OWNER_DECISION`, `PLATFORM_ADMIN_ALERT_DELIVERY`, `YOUTUBE_PLATFORM_API_PARTNERSHIP`, `DEPLOYED_LOG_METRICS_PIPELINE_CONFIG`, `PRODUCTION_EMAIL_DELIVERY`, `PRODUCTION_EMAIL_CREDENTIAL_ROTATION`.
  Plus the two source-derived signals the gate script evaluates before this register: `PRODUCTION_CRYPTO_SUITE` (from `backend/src/main.ts`; scoped to PARENT_C/ANDROID_D/IOS_FUTURE) and `REAL_UAT` (from `uat_execution_log.json`; scoped per target, and structurally unsatisfiable for PUBLIC_A/IOS_FUTURE/BILLING_FUTURE until the owner decides how those targets satisfy it).
- **IOS_FUTURE** -- 9 hard: `CRYPTO_SECURITY_REVIEW`, `IOS_MAC_XCODE`, `IOS_FAMILY_CONTROLS_ENTITLEMENT`, `IOS_PHYSICAL_DEVICE`, `PRODUCTION_CRYPTO_SECURITY_REVIEW`, `CRYPTO_ACTIVATION`, `PRODUCTION_CRYPTO_SUITE`, `REQUIRES_ENTITLEMENT`, `DEPLOYED_TLS_TERMINATION_CONFIG`; 5 conditional: `YOUTUBE_MODE_B_POLICY_REVIEW`, `CLOUD_AI_OWNER_DECISION`, `YOUTUBE_PLATFORM_API_PARTNERSHIP`, `PRODUCTION_EMAIL_DELIVERY`, `PRODUCTION_EMAIL_CREDENTIAL_ROTATION`.
  Plus the two source-derived signals the gate script evaluates before this register: `PRODUCTION_CRYPTO_SUITE` (from `backend/src/main.ts`; scoped to PARENT_C/ANDROID_D/IOS_FUTURE) and `REAL_UAT` (from `uat_execution_log.json`; scoped per target, and structurally unsatisfiable for PUBLIC_A/IOS_FUTURE/BILLING_FUTURE until the owner decides how those targets satisfy it).
- **BILLING_FUTURE** -- 7 hard: `SUPPORTED_CHARGE_CURRENCIES`, `SUPPORTED_SETTLEMENT_CURRENCIES`, `PAYMENT_PROVIDER_SELECTION`, `MERCHANT_ACCOUNT_APPROVAL`, `SETTLEMENT_BANK_CONFIGURATION`, `DEPLOYED_TLS_TERMINATION_CONFIG`, `PAYMENT_PRODUCTION_CERTIFICATION`.
  Plus the two source-derived signals the gate script evaluates before this register: `PRODUCTION_CRYPTO_SUITE` (from `backend/src/main.ts`; scoped to PARENT_C/ANDROID_D/IOS_FUTURE) and `REAL_UAT` (from `uat_execution_log.json`; scoped per target, and structurally unsatisfiable for PUBLIC_A/IOS_FUTURE/BILLING_FUTURE until the owner decides how those targets satisfy it).

## Rule

No agent, script, or lane may set any of these to CLOSED. They are closed only
by the accountable human owner named in `external_gate_matrix.json`, with an
evidence reference filled into that file's `evidence` field.
`tooling/release/Invoke-ReleaseGateCheck.ps1` treats any status other than
`CLOSED` as a HARD blocker only for release target(s) named in that gate's own
`releaseScope` array; a target named only in `conditionalReleaseScope` is
surfaced as a real but non-hard dependency, and a target named in neither
array is unaffected by that gate entirely.
