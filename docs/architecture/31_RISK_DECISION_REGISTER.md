# 31 — Risk and Decision Register

## Decision classification rule

Open decisions are visible and classified: **PRE-IMPLEMENTATION BLOCKER** must be approved before its named phase; **PRODUCT DEFAULT DECISION** may remain proposed at A-100 when alternatives, recommendation and phase are specified; **EXTERNAL DEPENDENCY** has a fallback; **A-100 BLOCKER** means architecture cannot be safely specified without a ruling.

| ID | Decision / risk | Classification | Status, recommendation and consequence |
|---|---|---|---|
| ADR-001 | Native Kotlin Android and Swift iOS | ACCEPTED BASELINE | Native OS controls; PCA-0 establishes workspaces after authorization. |
| ADR-002 | Parent/child roles and local, E2EE family store | ACCEPTED BASELINE | No readable activity service. |
| ADR-003 | Separate signing, key-agreement and FDEK roles; recovery envelope and trust epochs | ACCEPTED BASELINE | PCA-1 must implement docs 08–11 as one protocol. |
| ADR-004 | Android Standard and Protected modes | ACCEPTED BASELINE | Controls disclose availability; Protected assertions require managed authority. |
| ADR-005 | iOS Family Controls / Managed Settings / Device Activity | ACCEPTED BASELINE | Opaque tokens and entitlement constraints remain visible. |
| ADR-006 | Deterministic filtering before optional on-device AI; no covert TLS interception | ACCEPTED BASELINE | PCA-5/PCA-14 only process legitimately visible data. |
| ADR-007 | Arabic and English, full RTL at launch | ACCEPTED BASELINE | PCA-16 acceptance requires both independently selectable. |
| ADR-008 | Retention windows: 14d, 1m, 3m, 6m, 9m | ACCEPTED BASELINE | UTC events; calendar-month policy context; monotonic durations. |
| PCA-DEC-001 | Android Protected provisioning/distribution approach | PRE-IMPLEMENTATION BLOCKER | Approve before PCA-2 Protected Mode work; fallback is honest Standard Mode. |
| PCA-DEC-002 | Family Controls entitlement | EXTERNAL DEPENDENCY | Apply before PCA-15; fallback is no iOS restriction claim beyond public available capability. |
| PCA-DEC-003 | First-enrollment retention default | PRODUCT DEFAULT DECISION | Proposed 1 month; alternatives are the five windows; configure in PCA-1/12. |
| PCA-DEC-004 | Device-class battery budget | PRODUCT DEFAULT DECISION | Benchmark in PCA-2/15; disclose measured tiered budget, never invent a number. |
| PCA-DEC-005 | Controlled YouTube Mode B launch | PRE-IMPLEMENTATION BLOCKER | Decide before PCA-6; fallback is Mode A duration-only visibility. |
| PCA-DEC-006 | Recovery material custody UX | PRE-IMPLEMENTATION BLOCKER | Approve before PCA-1; no support master key; lost sole recovery secret means new family. |
| PCA-DEC-007 | Adult/child legal and regional onboarding disclosures | PRODUCT DEFAULT DECISION | Legal copy review before PCA-17; no hidden monitoring regardless. |
| PCA-DEC-008 | Any safety/security ambiguity in recovery or trust model | A-100 BLOCKER | None currently recorded as unresolved: docs 08–11 specify the protocol. |
| PCA-DEC-009 | Battery-impact disclosure number (PCA-NFR-034) -- benchmarked percentage vs qualitative statement | OPEN -- PROPOSED | Defined in doc 04. Gated behind real-device benchmarking (ANDROID_REAL_DEVICE_UAT). Gate token `PCA_DEC_009_DISCLOSURE_TEXT`. |
| PCA-DEC-010 | Default-uncertain classification timeout (PCA-NFR-032) | OPEN -- PROPOSED | Defined in doc 04. Moot while AI is deferred POST_V1. |
| PCA-DEC-011 | Minimum TLS version floor (PCA-NFR-001) | OPEN -- PROPOSED | Defined in doc 04. Device-support matrix decision; interacts with `DEPLOYED_TLS_TERMINATION_CONFIG`. |
| PCA-DEC-012 | Sync-overdue / policy-stale warning threshold | OPEN -- PROPOSED | Defined in doc 05, which carries a literal REQUIRES_FURTHER_OWNER_DECISION marker. |
| PCA-DEC-013 | Whether Relay operation may be delegated to a third-party managed-relay subprocessor | OPEN -- PROPOSED | Defined in doc 05. Merges into the RELAY_METADATA_PRIVACY and data-controller decisions. |
| PCA-DEC-014 | Multi-user / work-profile Android devices | OPEN -- PROPOSED | Defined in doc 06. Sub-decision of the Android provisioning question; only needed if Protected Mode ships. |
| PCA-DEC-015 | Root/compromise-indicator threshold for parent notification | OPEN -- PROPOSED | Defined in doc 06. Sub-decision of the Android provisioning question. |
| PCA-DEC-016 | iOS support scope for a child device without a configured Apple child account | OPEN -- PROPOSED | Defined in doc 07. Doc 07 makes resolving it a precondition for the iOS implementation phase. Tracked in NO gate register before PPR-1R. |
| PCA-DEC-017 | Custom `ManagedSettingsUI` shield branding at launch vs Apple's default shield | OPEN -- PROPOSED | Defined in doc 07. Note: the capability matrix records this as already decided; doc 07 still says PROPOSED. One of the two is wrong. |
| PCA-DEC-018 | Family Owner role transfer / succession | OPEN -- PROPOSED | Defined in doc 08. Doc recommends (a) not supported in v1. |
| PCA-DEC-019 | Whether a new parent-device invite requires identity verification beyond email/account confirmation | OPEN -- PROPOSED | Defined in doc 08. Largely settled in practice by the approved PCA-DEC-026 Option C. |
| PCA-DEC-020 | Forward-secrecy construction for message/session keys | OPEN -- PROPOSED | Defined in doc 09. THE critical-path decision: gates device sessions, policy delivery and all family sync. ~44 requirements depend on it. |
| PCA-DEC-021 | Exact RS encoding / recovery KDF, and whether a threshold recovery option is offered | OPEN -- PROPOSED | Defined in doc 09. Gates recovery only. PPR-1 recommends scoping it into the SAME reviewer engagement as PCA-DEC-020. |
> **PPR-1R correction — this register had a 13-row hole and an ambiguous namespace.**
>
> `PCA-DEC-009` through `PCA-DEC-021` were defined in their owning documents (04, 05, 06, 07, 08, 09)
> but appeared nowhere in this register, which jumped straight from `008` to `022`. They are added
> below with the subjects their owning documents give them.
>
> **Separately, and NOT yet resolved:** the `PCA-DEC-001`..`PCA-DEC-008` rows ABOVE use subjects that
> disagree with the canonical per-document definitions — the block is shifted, so `PCA-DEC-002`,
> `-004` and `-006` mean different things here than in docs 01, 02 and 03. Source code follows BOTH
> schemes. **Do not sign off any decision by number until that collision is reconciled;** the rows
> added below deliberately use the owning-document meaning, which is what the source and the
> capability matrix use.
| PCA-DEC-022 | FREE_STARTER entry-tier default (`parentMemberLimit = 1`, `managedDeviceLimit = 1`, price = FREE) for new registrations, introduced by Addendum 002 | PRODUCT DEFAULT DECISION | `OWNER_APPROVED`. Does not contradict/delete `PCA-FR-004A`'s base-family-tier requirement (2+ parents, 2+ children with ≥1 device each) — FREE_STARTER is a narrower on-ramp tier below the paid/standard base tier, not a redefinition of it. Any increase above `managedDeviceLimit = 1` is potentially billable per the Addendum 002 price-book model. Configure in PCA-PA-2 (Addendum 002). |
| PCA-DEC-023 | Entitlement-count separation: `parentMemberLimit` and `managedDeviceLimit` modeled as two independent counters, never a combined "user/phone" count (Addendum 002 Section 6) | PRE-IMPLEMENTATION BLOCKER | `OWNER_APPROVED`. Approve before PCA-PA-2 (Addendum 002) entitlement-write implementation; fallback if ever revisited would require a recorded architecture change, not a silent implementation shortcut. |
| PCA-DEC-024 | Commercial pricing/currency/quote model for device-entitlement increases (Addendum 002 Sections 7, 9, 10, 18): App-Owner-maintained versioned price book keyed by commercial market × currency × target device limit; standard-priced vs. custom-quote upgrade paths; initial currencies USD/SAR/YER (EUR explicitly out of initial scope); no automatic FX conversion required for initial release; quote snapshot (`targetDeviceLimit`/`amountMinor`/`currencyCode`/`priceBookVersion`) immutable once attached to a request; `managedDeviceLimit` always server/admin-authoritative, never parent-writable; entitlement increase only on authoritative server-side/webhook payment confirmation, idempotent per payment/provider event ID | PRE-IMPLEMENTATION BLOCKER | `OWNER_APPROVED`, issued as a coordinator amendment during Addendum 002's authoring (2026-08-14), superseding an earlier internal draft that had used an illustrative four-currency (USD/YER/EUR/SAR) list. Approve before PCA-BILL-1/PCA-BILL-2 (Addendum 002) implementation; fallback is no billable entitlement increase (FREE_STARTER-only operation) until the price-book and payment-confirmation mechanism exist. |
| PCA-DEC-025 | Server-verifiable Family-Owner commercial authority (PCA-FAMILY-AUTH-1): the backend has no trust anchor, FTS version, or role-proof format that lets it independently verify FAMILY_OWNER without a new cryptographic artifact — see `docs/implementation/decisions/PCA_IMPL_DECISION_002_FAMILY_COMMERCIAL_AUTHORITY.md` for the full inventory and three options (A: genesis-anchored owner attestation chain; B: interactive step-up co-signature reusing `deviceauth`; C: decouple commercial authority from FTS role, use account-of-record instead) | PRE-IMPLEMENTATION BLOCKER (for production wiring only) | `OWNER_APPROVED_OPTION_A` (2026-08-14, PCA-FAMILY-AUTH-1-R1). Option B/C explicitly not selected. Source-complete and tested: `backend/src/familycommercial/authority/` (genesis anchor + append-only signature-verified Owner-attestation chain) plus `AttestationChainFamilyCommercialAuthorityResolver` (`backend/src/billing/authority/`), migration `0011_family_commercial_authority.sql`. Full required test matrix (tamper/transfer/stale/revoked/cross-family/cross-member, real-MySQL genesis-race and chain-head-race concurrency across two independent clean rooms) passes. `UnavailableFamilyCommercialAuthorityResolver` remains the actual production default pending a Coordinator-performed `main.ts` wiring change (SHARED_INTEGRATION_REQUIRED, not done by this lane). Approve/verify the production wiring change before treating FAMILY_OWNER-gated checkout as cryptographically enforced in production. |
| PCA-DEC-026 | Parent/family identity model and browser-reachable service-session issuance (PCA-AUTH-SESSION-1): no verified-identity primitive exists anywhere for the parent/family plane — `AuthService.issueSession(VerifiedIdentity)` has zero call sites, and `parent-web`'s existing `RealServiceAuthClient` targets a `/api/auth/login` cookie contract that was never implemented server-side (a speculative frontend guess, not an approved architecture) — see `docs/implementation/decisions/PCA_IMPL_DECISION_003_PARENT_ACCOUNT_IDENTITY.md` for the full option inventory (A: passkey/WebAuthn; B: trusted-browser relay extending the Family Trust Set device-trust model; C: self-service email+password registration with email-verification code; D: defer) | PRE-IMPLEMENTATION BLOCKER | `OWNER_APPROVED_OPTION_C` (2026-08-15, Round5 pre-flight). Self-service registration: email + password + confirm-password, one-time email verification code, first-verified-parent auto-initializes a new family and becomes its initial Family Owner via the existing genesis/bootstrap process, browser service session issued only after email verification, account receives the platform-configured FREE_ACCESS policy at registration, no admin approval required. Explicitly does NOT collapse service-session identity with Family Owner commercial authority — `PCA-DEC-025` Option-A remains the sole authority for FAMILY_OWNER-gated commercial mutations; a service session only ever proves "recognized, email-verified account," never a family role. See `PCA_ADDENDUM_003_PARENT_IDENTITY_REGISTRATION_FREE_ACCESS.md` for full normative requirements, and reconciles with `PCA-DEC-022` (`FREE_STARTER` entitlement defaults) — FREE_ACCESS is a distinct, broader commercial-access policy that a registration snapshots alongside (not instead of) the FREE_STARTER entitlement defaults. |
| PCA-DEC-027 | Replay-retention policy for the durable action-idempotency ledger (`action_idempotency_ledger`, migration 0047, `MySqlActionIdempotencyLedger`): how long, and under what ceiling, must a recorded `(scope, idempotency_key)` authorization outcome be retained — and what does a MISS mean to each caller? Raised by the independent security review of the P1-04 Wave 1 change (finding SEC-2). The table's ceiling is one GLOBAL bound of `ACTION_IDEMPOTENCY_LEDGER_CAPACITY` (4096) rows — a SOFT ceiling, since the trim is best-effort and the row just written is excluded from its own trim, so the table can transiently sit at capacity+1 — shared by every family scope AND the platform emergency-directive scope, so a busy owner can evict another owner's, or the platform's, replay record before its window has elapsed — degrading exactly-once to at-most-once for the evicted key. Note precisely what is and is NOT at stake: eviction cannot let one owner READ or DISPLACE another's entry (that is the `(scope, idempotency_key)` primary key's job), and it cannot forge a verdict; it removes a remember-me, not a check. | OPEN -- PROPOSED | Genuinely undecided, and deliberately NOT resolved by the implementer: choosing a TTL or a per-scope bound to make the code look finished would be inventing product/security policy, which is forbidden. Per-scope bounds are not a drop-in fix either — the number of scopes is unbounded, so a per-scope bound bounds nothing unless the per-scope retention WINDOW is also fixed, which is the decision. Options: (a) keep the single global ceiling and accept at-most-once beyond it, disclosing the window implied by expected write volume; (b) fix a retention window per scope and trim by age rather than by global row count (self-bounding, needs a stated window and a decision on whether a replayed-but-evicted action is refused or re-evaluated); (c) move retention to per-scope storage or a partition-per-owner model. Recommendation: (b), because it makes retention a stated duration rather than an emergent consequence of traffic mix and it is the only option of the three whose guarantee does not silently depend on how busy other families are. Consequence of leaving it open: the P1-04 finding may be closed as "durable across restarts and instances" but NOT yet as "exactly-once per scope". Current source states the limitation honestly in `MySqlActionIdempotencyLedger`'s and `ActionIdempotencyLedger`'s doc comments, pins the global-bound behaviour (not the per-scope guarantee) in `test/familyrbac/ActionIdempotencyLedger.test.mjs`, and executes the real trim DELETE against MySQL 8.4 in `test/db/actionIdempotency.mysql.test.mjs`, so the gap is visible rather than latent. |
| PCA-DEC-028 | E2EE posture of child-request and bonus-grant content, and the shape of their durability fix. `backend/src/childrequests/ChildRequestRepository.ts` documents itself as "Local/E2EE persistence port -- never a central readable store of child request content", and `BonusGrantLedger.ts` as family-policy content that `contracts/schedule-runtime/SchedulePolicyV1.md` "treats as E2EE-only". Neither describes the running system. `ChildRequest` carries real family content -- `reasonNote` (child-authored free text), `requestedAppScope`, `installTargetPackageName`, `installTargetAppLabel` (which app, by name) -- and `childRequestRoutes.ts`'s `toRequestDto` serialises every one of those fields, with `GET /api/parent/families/:familyId/child-requests` returning them as plaintext JSON on a family session, out of a process-local plaintext store wired into the production composition root (`main.ts`). `main.ts`'s own note is narrower and remains true: it claims only that this lane introduces no new central plaintext MySQL TABLE. This is the same content/metadata boundary `docs/product-completion/PCA_FAMILY_AUTHORITY_COMPLETION_ARCHITECTURE.md` resolved for audit events -- where the answer was opaque-envelope-only -- reached from the opposite direction, in a sibling subsystem that currently does the readable thing. | OPEN -- PROPOSED | Two genuinely different fixes, which is why this is not closed by an implementer: (a) move child-request content to the audit-event/alert posture -- an opaque, crypto-bound envelope with the authoritative record on the family's own devices and the plaintext parent-web DTO replaced by local decryption; or (b) accept that a child-request's content is served in the clear to an authenticated parent session, and re-state the module contracts accordingly. (a) is the posture doc 18's model implies and is consistent with every other family-content surface, but it is real work and it is gated by the same `CRYPTO_SUITE` review as every other E2EE surface; (b) is cheap and needs no crypto, but must then be an explicit, reviewed product position rather than an unexamined default. Recommendation: (a), for consistency with audit events and alerts -- a family-content boundary that holds on one subsystem and not its sibling is not a boundary. Consequence of leaving it open: the module comments overstate the guarantee. That false assurance was corrected in source under this change (the comments now state what is actually true and cite this decision), so no reader is misled, but the underlying exposure -- server-side plaintext of family content, readable by anything that can read server memory -- remains. BINDING REGARDLESS OF WHICH OPTION IS CHOSEN: P1-04's tracked debt for `InMemoryChildRequestRepository` must NOT be discharged with a plaintext table. Restart loss there is real (a parent's request list silently becomes empty, reading as a confident "no requests" rather than as unavailable), so the debt stands -- but "make it durable" is not a licence to persist `reasonNote` or app identity server-side in the clear. `BonusGrantLedger` is classified separately and does NOT require durability: losing it cannot grant time that was never granted or weaken enforcement, because the enforcement source of truth is always the device's own persisted `SchedulePolicyV1`. |
| PCA-DEC-029 | Which capabilities the family dashboard shows at all: `DashboardAggregatorService.resolveVisibleKinds` derives the visible card set FROM the registered providers (`this.providers.keys()`), not from the 15 `DashboardCardKind` values doc 18 Section 6 declares as the navigation list. A capability whose lane has no provider wired is therefore ABSENT from the dashboard entirely — no tile, no card, nothing saying so — so a parent cannot distinguish "this capability is unavailable" from "this capability is not in this build". Evidence: `backend/src/parentpanel/DashboardAggregatorService.ts`; `main.ts` registers only `WebFilteringDashboardCardProvider` and `YouTubeDashboardCardProvider`, so 13 of 15 declared kinds are currently silent; the aggregator's own `provider === undefined` branch is UNREACHABLE for the same reason. This is the same silent-absence-vs-honest-unavailability distinction previously fixed in `WebFilteringDashboardCardProvider`, reached from a different direction. | OPEN -- PROPOSED | Closing it means emitting a card for every declared kind, i.e. adding 13 new UNAVAILABLE tiles to the parent dashboard — a product/UX decision with real surface area, not an implementation tidy-up, which is why it was NOT changed unilaterally while fixing the sibling card. The behaviour is now pinned by an explicit `KNOWN GAP` test (`backend/test/parentpanel/DashboardAggregatorService.test.mjs`) so it cannot be mistaken for the honest-unavailable behaviour. Options: (a) emit all declared kinds, UNAVAILABLE when unwired (maximally honest, largest UI change); (b) declare a per-release SUPPORTED card set and emit UNAVAILABLE only within it, accepting that unsupported capabilities are legitimately absent; (c) leave as-is and record it as accepted. Recommendation: (b) — it is the only option that answers "absent on purpose" vs "absent by accident", which is the actual question, and it keeps the release's honest scope explicit rather than inferred from wiring. |
| PCA-DEC-030 | What a parent is told when WEB FILTERING cannot reach a verdict. `WebFilterEngine.decide` ALLOWs a request when the VPN/enforcement layer reports unavailable coverage, carrying only the machine reason code `network filtering capability was unavailable for this request`; `WebFilterClassifierAdapter` likewise falls through so an unavailable AI classifier on an unmatched domain yields `ALLOW`/`DEFAULT`. Both are deliberate, documented house posture (`backend/src/web/types.ts:99-100` records that the ALLOW is indistinguishable from a policy ALLOW except by that machine key). But an ALLOW is never persisted as a block decision (`SafeBrowserNavigationPolicy`), so the parent-visible consequence is a dashboard showing zero blocks — byte-identical to "nothing happened" — while the child reaches content the filter exists to block. Note the same repo's schedule engine explicitly refuses this posture (`backend/src/schedule/engine.ts:22`: "enforcement-unavailable never silently allows"), so two sibling enforcement surfaces disagree today. | OPEN -- PROPOSED | NOT changed here: whether enforcement-unavailable should fail OPEN or fail CLOSED is a product safety decision (fail-closed means an outage blocks legitimate content family-wide, which has its own harm), and doc 14's denylist model may make default-allow the intended reading. What is NOT defensible either way is the parent-visible truth: a parent currently cannot tell "filtering was unavailable for this period" from "no site was blocked". Options: (a) keep default-allow, but surface an explicit capability state (e.g. the WEB_FILTERING card reporting UNAVAILABLE/`SYNC_STALE` for the period) so the parent is told; (b) match the schedule engine and fail closed; (c) keep as-is and record the divergence as accepted. Recommendation: (a) — it preserves the availability behaviour the existing tests pin while removing the confident-wrong parent signal, and it is the smaller change of the three. |
| PCA-DEC-031 | Five production-path gaps surfaced by a systematic test-suite audit (`STORE_TEST_PASS != PRODUCTION_PATH_PASS`), each currently expressed as a passing test rather than as a tracked gap. (1) Rule-package rollback does not retract: `SignedRulePackageConsumer.apply` is additive-only and never calls `repository.remove()`, so a rule the rolled-back package no longer contains stays enforced, while `rollbackOutcome.status === 'APPLIED'` — `backend/test/web/SignedRulePackageConsumer.test.mjs:116` pins this as `KNOWN GAP ... a future fix intentionally flips this assertion`. (2) The entitlement slot hook RESERVED -> CONSUMED has no caller anywhere: `backend/test/db/platformEntitlementsSlots.mysql.test.mjs:211` proves the transition by driving the repository directly and its own NAME states "not wired to any caller", so in production a reserved slot never becomes active and entitlement/utilisation counts stay wrong after enrolment. (3) `MySqlOwnerParentDeviceResolver` resolves the Owner device only (`backend/src/alerts/MySqlOwnerParentDeviceResolver.ts:23-30`, documented `KNOWN GAP`), so a protection/degradation alert reaches at most one parent device, while `backend/test/db/protectionAlerts.mysql.test.mjs:126` names it as "the sole recipient". (4) `NoopFamilyMemberAccountBinder` is blessed as "safe" by a test asserting only `doesNotReject` (`backend/test/familymembers/service.test.mjs:172-174`), so an inviter that forgets to inject the real binder silently does not bind the accepting account; `main.ts` does wire `MySqlFamilyMemberAccountBinder`, limiting today's blast radius. (5) No FK constrains `child_profiles.family_id` and `backend/test/db/childProfileRegistry.mysql.test.mjs:44` asserts the absence as intended behaviour, so a caller bypassing the application-layer check creates orphan child profiles and no test objects. | OPEN -- PROPOSED | Each is recorded rather than fixed because the fix is a design decision, not a tidy-up, and this pass deliberately did not invent policy: (1) true snapshot rollback changes what `apply` guarantees and needs its own review (over-blocking is at least the fail-safe direction, which is why it is not treated as a security defect); (2)(3) require deciding the real writer/recipient set, and (3) is additionally bounded by the same `PCA-DEC-025` authority-attestation wiring that already gates protection alerting; (4) needs a decision on whether the no-op default should be constructible at all; (5) is an explicit, documented charset/cross-plane trade-off (`docs/pre-production/PCA_PPR2_OWNER_DECISIONS.md:345`), so adding the constraint is a schema change with its own review. Consequence of leaving these open: the suite reports them as PASSING behaviour rather than as gaps, which is the condition that lets them persist — so each is named here with its evidence, and (2) is the one with live functional impact today. |

## Residual risks

| Risk | Impact | Mitigation / gate |
|---|---|---|
| Android authority, OEM background limits, VPN disable | High | capability matrix, health/degraded state, Standard fallback; validate PCA-2/17. |
| Apple entitlement / opaque activity model | High | entitlement application, explicit fallback and capability labels; validate PCA-15/17. |
| Stolen/offline device or recovery-code theft | High | signed epoch rotation, recovery authentication, stale/revoked state; no retroactive wipe claim. |
| E2EE support/recovery complexity | High | recovery envelope, replacement-device procedure, no support decryption; test PCA-1/17. |
| Classifier error or malicious rules/models | Medium | deterministic precedence, signing, rollback and appeal path. |
| Platform/policy/source changes | High | doc 33 180-day source revalidation before each affected phase. |
| Architecture status documents drifting from actual implementation state | Medium | Repository survey conducted 2026-08-14 (`PCA-DOC-REALIGN-1`) found `docs/architecture/30_IMPLEMENTATION_PROGRAMME.md`, `PCA_ARCHITECTURE_MASTER_v1.0.md`, `docs/architecture/README.md`, `MANIFEST.md`, and `34_ARCHITECTURE_COMPLETION_GATE.md` all still asserting `IMPLEMENTATION_STATUS = NOT_STARTED` despite substantial source (~33,000 lines Android, ~16,800 lines backend, ~10,600 lines Parent Web) already existing. Mitigated for this pass by realignment to the `SOURCE_COMPLETE`/`VALIDATED_COMPLETE`/`PRODUCTION_READY` framework (doc 00 Section 8A); ongoing mitigation requires updating doc 30/32 status alongside code changes rather than only at periodic realignment passes — this is a process discipline gap, not a one-time fix. |
| Device-slot concurrency race (two simultaneous enrollment attempts consuming the same last entitlement slot) — Addendum 002 Section 8 | High | Mandatory atomic reserve-if-under-limit operation (`PCA-ADD-PA-038`) and a release-blocking concurrent-reservation race test (`PCA-ADD-PA-039`); no count-then-insert pattern permitted. Validate in PCA-PA-2 (Addendum 002). |
| Payment/webhook forgery, replay, or double-application of an entitlement increase — Addendum 002 Sections 13, 8.1 | High | Provider signature verification, idempotency keyed to provider event ID, replay/freshness checks, server-side status authority over client redirects (`PCA-ADD-BILL-030`–`035`, `PCA-ADD-BILL-046`). Validate in PCA-BILL-2/PCA-PA-5 (Addendum 002). |
| Payment secret mishandling (raw card/bank data, unredacted provider secrets) — Addendum 002 Section 11 | High | No-raw-PAN/CVV/bank-credential storage, provider-hosted/tokenized checkout only, secrets-manager-only credential storage, privacy-absence testing extended to the Billing domain (`PCA-ADD-BILL-023`–`026`). Validate in PCA-BILL-2/PCA-PA-5 (Addendum 002). |
| Platform-admin authority silently collapsing into family RBAC or a bare `isAdmin` flag — Addendum 002 Section 4 | High | Mandatory separate account type, session type, RBAC, MFA, step-up, and audit for every Platform Administration role; `isAdmin=true`-style flags on a family account explicitly rejected as sufficient authority (`PCA-ADD-PA-010`–`020`). Validate in PCA-PA-1 (Addendum 002). |


## Legacy register series and namespace reconciliation (2026-09-08)

The PPR-1R correction above recorded that rows `PCA-DEC-001`..`PCA-DEC-008` in this register use subjects that disagree with the canonical per-document definitions. This section **resolves** that collision rather than restating it. Under doc 00 Section 4's authority order the owning document governs its own subject, so the **owning-document definitions are canonical** and this register's original eight rows are re-keyed as a *legacy register series* `PCA-DEC-R01`..`PCA-DEC-R08`. No owning-document ID is renumbered; no decision status changes.

| Register row (legacy key) | Register subject | Canonical ID with that subject | `PCA-DEC-00n` canonical (owning-document) subject | Source/docs references and which meaning they use |
|---|---|---|---|---|
| `PCA-DEC-R01` (was 001) | Android Protected provisioning/distribution approach | `PCA-DEC-002` (doc 01) + `PCA-DEC-014/015` (doc 06) | **`PCA-DEC-001` = legal data-controller role for family activity data (doc 01 §11)** | `ProtectedModeProvisioningGate.kt`, `RebuildR3DerivedLedgers.mjs` cite `PCA-DEC-002/014/015` — canonical meaning. |
| `PCA-DEC-R02` (was 002) | Apple Family Controls entitlement | gate `IOS_FAMILY_CONTROLS_ENTITLEMENT` (no owning-doc decision ID; doc 07 treats it as an external dependency) | **`PCA-DEC-002` = Android Protected Mode distribution legality/mechanism (doc 01 §11)** | Android source uses the canonical meaning. |
| `PCA-DEC-R03` (was 003) | First-enrollment retention default | `PCA-DEC-003` (doc 01 §11, doc 03 `PCA-FR-101`, doc 11) — **same subject in both schemes** | `PCA-DEC-003` = default first-enrollment retention window | `retentionRoutes.ts`, `parent-web/src/api/interfaces.ts` — canonical, unambiguous. |
| `PCA-DEC-R04` (was 004) | Device-class battery budget | `PCA-DEC-009` (doc 04, `PCA-NFR-034`, gate `PCA_DEC_009_DISCLOSURE_TEXT`) | **`PCA-DEC-004` = adult-authentication strength for a new Parent role invite (doc 02 §7)** | No source reference. |
| `PCA-DEC-R05` (was 005) | Controlled YouTube Mode B launch | gate `YOUTUBE_MODE_B_POLICY_REVIEW`; doc 03 §D routes Mode B through that gate, not a numbered decision | **`PCA-DEC-005` = Viewer role in base tier vs gated (doc 02 §7)** | No source reference. |
| `PCA-DEC-R06` (was 006) | Recovery material custody UX; no support master key | doc 09 Section 5 / ADR-003 (baseline, not an open decision); recovery KDF/threshold is `PCA-DEC-021` (doc 09) | **`PCA-DEC-006` = whether PCA may ever use an authenticated child's YouTube account-history surface (doc 03 §D)** | `backend/src/recovery/types.ts` cited `PCA-DEC-006` with the *legacy* meaning; corrected 2026-09-08 to cite doc 09 §5 / `PCA-DEC-R06`. |
| `PCA-DEC-R07` (was 007) | Adult/child legal and regional onboarding disclosures | owner decision OD-13 (legal facts) tracked in `docs/public/reports/RELEASE_A_LEGAL_OWNER_INPUT.md` and `PCA_PPR2_OWNER_DECISIONS.md`; no owning-doc decision ID | **`PCA-DEC-007` = default-uncertain content policy (doc 03 §D.1)** | No source reference. |
| `PCA-DEC-R08` (was 008) | Safety/security ambiguity in recovery or trust model (A-100 blocker; none recorded) | closed at A-100; no successor | **`PCA-DEC-008` = Standard Mode install-approval report-only vs Accessibility-Service interception (doc 03 §D)** | `ChildRequestService`/install-approval code follows doc 03's `CAPABILITY_HONEST_INSTALL_APPROVAL` decision (D2) — canonical meaning. |

Rule going forward: any reference to `PCA-DEC-001`..`PCA-DEC-008` means the owning-document subject in the fourth column; anyone needing the register's original subject cites the `PCA-DEC-R0n` key. The table at the top of this file is preserved verbatim as the historical record and must be read through this crosswalk.

**`PCA-DEC-017` contradiction, restated for the owner:** the capability matrix records the `ManagedSettingsUI` shield-branding decision as decided while doc 07 still says `PROPOSED`. Not resolved here — it is the owner's ruling to record, and iOS is not on any current release ladder.
