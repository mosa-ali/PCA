# 30 — Implementation Programme

**Status note (final independent assessment `PCA-FINAL-ASSESSMENT-2026-09-08`, HEAD `5dacd84` + this pass):** this document was last realigned on 2026-08-14 (`PCA-DOC-REALIGN-1`) and had drifted badly since. In particular it asserted, "on the strength of a repository survey", that *no Platform Administration or Billing source exists anywhere in the repository* and that every Addendum 002 workstream was `NOT_STARTED` on all three tiers. That was false at the time of the 2026-09-05 FABLE audit and is false today: `backend/src/{platformadmin,billing,entitlements,familycommercial,commercialmaintenance,commercialnotifications}`, `platform-admin-web/` (31 test files, 153 passing tests) and twelve-plus billing migrations exist and are exercised by the CI-run backend suite. Every row below was re-derived on 2026-09-08 against executed evidence (see `docs/supervision/PCA_FINAL_GAP_ASSESSMENT_2026-09-08.md` for the evidence log). The earlier realignment note is preserved in git history; nothing about the A-100 architecture acceptance changes.

## Completion framework

Every phase is reported on three independent tiers (doc 00 Section 8A). The tiers are never inferred from one another.

- **`SOURCE_COMPLETE`** — code exists implementing the phase's scope. `YES`, `PARTIAL` (with the missing scope named), or `NO`.
- **`VALIDATED_COMPLETE`** — tested against acceptance criteria with recorded, reproducible evidence. Values used below: `PARTIAL` (automated unit/integration/DB evidence exists and was executed on 2026-09-08, but the phase's real-device, real-provider or real-human acceptance evidence does not), `NO` (no executed evidence), or `YES` only where every acceptance criterion is automated and executed. **No phase reaches `YES` today**, because doc 28's device/E2E, privacy-absence-on-device and UAT layers have never been executed (`docs/release_readiness/uat_execution_log.json` is `NOT_EXECUTED`, 0 of 54 cases).
- **`PRODUCTION_READY`** — every applicable doc 31/`external_gate_matrix.json` gate closed. All 39 registered gates are open, so **no phase is `PRODUCTION_READY`**.

Executed evidence behind the `PARTIAL` validations on 2026-09-08 (local, disposable environments; nothing deployed): backend non-DB suite 2,330 → 2,336 tests after this pass, 0 failures; backend MySQL 8.4 suite 532 tests, 528 pass, 4 privilege-gated skips; parent-web Vitest 996 tests, 0 failures; platform-admin-web Vitest 153, 0 failures; Android JVM 1,347 tests, 0 failures, 1 skipped; parent-web Playwright 65/68 (3 click-timeouts under host contention, re-run individually — see the assessment); platform-admin-web Playwright 15/15; four contract catalogues validated (the runtime-sync validator now reconciles routes and error codes against source); release-gate tooling 0 failures across all six targets.

## Phase status (re-derived 2026-09-08)

| Phase | Scope | SOURCE_COMPLETE | VALIDATED_COMPLETE | PRODUCTION_READY | Evidence / remaining gap |
|---|---|---|---|---|---|
| **PCA-0** — Repository and quality foundation | Workspaces, contracts, CI, release tooling | YES | PARTIAL | NO | `.github/workflows/quality-gates.yml` runs backend build+unit, both web unit suites (sharded), Android build/lint/unit, contracts, security, repo checks, release-control parity. Not in CI: the MySQL suite (credential-free by design). Playwright e2e for both consoles runs in CI since `568a4c7` (job `web-e2e`); the Android job builds the R8-shrunk release variant unsigned since 2026-09-08 (signing needs the owner keystore, gate `ANDROID_RELEASE_SIGNING_CONFIG`). |
| **PCA-1** — Cryptographic enrollment and local vault | Family/device keys, QR pairing, encrypted local storage, recovery baseline | YES (backend `enrollment/pairing/deviceauth/invitation/familytrustset/familyenvelope`; Android `enrollment`, `persistence`) | PARTIAL (unit + MySQL tests; no device or reviewed-crypto execution) | NO | Every signature path is wired to `Rejecting*` verifiers by design (`PRODUCTION_CRYPTO_SUITE = PENDING_HUMAN_SECURITY_REVIEW`). Android persists `familyId = ""` at enrollment because the bootstrap DTO never returns one by design (`EnrollmentCoordinator.kt`); since 2026-09-08 local retention no longer depends on it (see PCA-12). `backend/src/recovery` has no HTTP route (recovery cryptography is `PCA-DEC-021`-gated). |
| **PCA-2** — Android platform foundation | Standard Mode capability adapter, usage access, Protected Mode gate | YES (Standard); Protected Mode deliberately unprovisioned | PARTIAL (JVM tests only) | NO | `ProtectedModeProvisioningGate` reports `PENDING_OWNER_DECISION` by design (doc 01 `PCA-DEC-002`, doc 06 `PCA-DEC-014/015`). No `DeviceAdminReceiver` exists, by design. Real-device gates open. |
| **PCA-3** — Screen-time/break engine | Deterministic state machine, offline persistence, Break Shield, emergency exceptions | YES | PARTIAL (JVM; 0 of 43 device UAT cases) | NO | Break Shield is in-app; emergency floor and call handling need `REAL_DEVICE_TELEPHONY_UAT`. |
| **PCA-4** — App usage and schedule controls | Local evaluation, schedule runtime, enforcement | PARTIAL | PARTIAL | NO | Android schedule runtime wired; enforcement is a no-op without Device Owner (Standard Mode degradation). There is no backend usage module (the `backend/src/usage` directory is empty; usage stays on-device and E2EE by design -- the earlier "unreachable module" note was stale). Remote policy delivery is crypto-gated. |
| **PCA-5** — Web filtering and Safe Browser | Rule engine, Safe Browser UI, VPN/DNS stack | PARTIAL | PARTIAL | NO | Safe Browser, rule engine and rule storage complete. Device-wide VPN/DNS filtering is built but held **dormant** by `VpnEnforcementPolicy` (owner decision `ANDROID_VPN_DNS_POLICY`; hard-coded third-party resolver must be decided before any activation). Parent web rules are in-memory in production (`InMemoryWebRuleRepository`; durable persistence is a privacy-architecture decision, W3). |
| **PCA-6** — YouTube visibility | Mode A duration-only; Mode B flag-gated | YES (Mode A); Mode B inert by construction | PARTIAL | NO | Mode B stays off until `YOUTUBE_MODE_B_POLICY_REVIEW` (conditional gate). |
| **PCA-7** — Location and last-seen | Sampling, encrypted local persistence, geofence/safe zones | YES (Android runtime/location, geofence; backend safe zones; parent-web authoring) | PARTIAL | NO | Publishing safe zones to devices is crypto-gated. |
| **PCA-8** — Eye-distance/proximity protection | Sensor tier, camera tier, backend settings | YES (Android sensor + CameraX tiers with privacy static scan; backend `eyeprotection` routes, cross-family read fixed W1) | PARTIAL | NO | Camera behaviour across OEM stacks needs `CAMERA_REAL_DEVICE_VALIDATION`. |
| **PCA-9** — Prayer-time engine | Calculation, alarm scheduling, reminder receiver | YES (Android `feature/prayer`, `runtime/prayer`, manifest-registered receiver) | PARTIAL | NO | Backend has no prayer module by design. Real-time accuracy needs device UAT. |
| **PCA-10** — Parent dashboard, RBAC and child requests | Parent Web, family RBAC, child requests, membership, identity | YES | PARTIAL (996 Vitest + backend unit/MySQL; Playwright not in CI; 0 of 22 UAT cases) | NO | Server-side family-authority resolution is `UnavailableTrustSetRoleResolver` until crypto activates, so every trust-set-gated parent mutation is denied in production today. Child-request device-level existence oracle closed 2026-09-08. |
| **PCA-11** — E2EE relay synchronization | Envelope acceptance, relay, runtime sync, atomic acceptance | YES | PARTIAL (unit + MySQL atomic-acceptance tests) | NO | Envelope acceptance now fails closed **structurally**: the inline placeholder context (empty key, epoch floor 0) was replaced on 2026-09-08 by `rejectingResolveEnvelopeContext` (unattainable epoch floors), so activating a reviewed verifier without a real Family-Trust-Set resolver can no longer open an anti-downgrade hole. A durable trust-set store does not exist yet. |
| **PCA-12** — Retention/deletion and encrypted export | Retention policy intake, delete-now, export | PARTIAL | PARTIAL | NO | Delete-now ledger is durable (`MySqlDeleteNowLedger`, W3). **The retention-policy route stores nothing** — it validates and audits only, and since 2026-09-08 says so (`202`, `persisted: false`) instead of the previous false `accepted: true`. Export returns 202 `PENDING_CRYPTO_REVIEW`. Android local retention **runs for an enrolled-but-unpaired device** since 2026-09-08 (`RetentionEngine.runLocalDeviceCycle`, receipts stamped `LOCAL_DEVICE_UNPAIRED`; until then every locally captured row lived forever pre-pairing because the cycle required a family id the bootstrap never discloses) and judges expiry against the tamper layer's trusted-time floor so a rolled-back clock cannot postpone deletion. |
| **PCA-13** — Tamper detection and recovery hardening | Tamper monitors, recovery transaction bookkeeping | PARTIAL | PARTIAL | NO | Android tamper monitors are wired and write real events; `backend/src/tamper` has no route/consumer. Recovery cryptography is not implemented (crypto review). |
| **PCA-14** — On-device AI classifiers | Model lifecycle, classifier runtime | NO | NO | NO | No model binary or classifier engine exists on any platform; iOS has a typed fail-safe runtime boundary only. Deferred `POST_V1` pending `CLOUD_AI_OWNER_DECISION`. |
| **PCA-15** — iOS Family Controls implementation | Authorization, Managed Settings, Device Activity, extensions | PARTIAL (early) | NO | NO | 57 Swift files with protocol seams and 148 XCTest cases that have **never been built or run** (no macOS/Xcode; CI job is deterministically red with a `TEST_HOST` project error). The `PCADeviceActivityMonitor` extension target compiles one file referencing host-app-only types; no `DeviceActivityCenter.startMonitoring` call site; no `DEVELOPMENT_TEAM`, app icon or launch screen; entitlement/device gates open. |
| **PCA-16** — Arabic/English accessibility and UX closure | EN/AR parity, RTL, bidi, a11y | YES (parent-web, platform-admin-web, public-web and Android each at exact EN/AR key parity; RTL applied) | PARTIAL | NO | Real-browser WCAG AA contrast (EN + AR, with an Arabic-text audit axe cannot do) runs in CI for both consoles; forced-colours, `prefers-contrast: more` and reduced-motion are real-browser tested in both consoles since 2026-09-08 (`e2e/forced-colors.spec.ts`). Android `BidiUtils` is wired at the Safe Browser block screen (sanitised + isolated domain and reason code) and the enrollment key fingerprints, pinned by `MixedDirectionIsolationStaticTest`. Still no screen-reader or device evidence (`UAT-I18N-01..03`). |
| **PCA-17** — Security/privacy red team and store compliance | Scanners, red-team scripts, crypto review package, store submission | PARTIAL | PARTIAL | NO | Security scanner, dependency audit, SBOM and privacy schema gates run in CI. The production crypto review package exists but the findings document is an empty template — the review has not happened. Since 2026-09-08 a `release` build type exists (R8 minify + resource shrink, signing read from `PCA_RELEASE_*` environment values, unsigned when they are absent -- CI builds it every run); the owner keystore and a signed artifact's provenance are still missing, so `ANDROID_RELEASE_SIGNING_CONFIG` stays open. No Play/App Store metadata exists. parent-web's SBOM is now generated and validated in CI from the installed tree. |
| **PCA-18** — Family beta/UAT | Real-device/real-provider acceptance | NO | NO | NO | `uat_execution_log.json` honestly `NOT_EXECUTED`, 0/54. No cohort, no devices, no provider. |
| **PCA-19** — Production release | Release gate, containers, bootstrap runbook, deployment | PARTIAL | NO | NO | Gate tooling and containers for all four surfaces exist; the live-bootstrap runbook exists; **no deployment pipeline exists and nothing PCA has ever been deployed**. Azure holds a MySQL 8.4 server with an empty `pca_pro` database and a backend App Service that still runs a placeholder container (verified read-only 2026-09-08). |

## Addendum programmes (re-derived 2026-09-08)

Per-ID status lives in `docs/implementation/PCA_IMPLEMENTATION_TRACEABILITY.md` and `PCA_COMPLETION_V2_MATRIX.json`; the matrix counts below are computed from that JSON at this pass, not restated from the addenda's own (stale) headers, which were corrected in the same pass.

| Programme | Inventory | SOURCE_COMPLETE (incl. external-gate / validation-pending sub-statuses) | PARTIAL | NOT_STARTED | NOT_APPLICABLE | VALIDATED_COMPLETE | PRODUCTION_READY |
|---|---|---:|---:|---:|---:|---|---|
| Addendum 001 — Secure invite / protected enrollment / Administration PIN (`PCA-ADD-ENR-*`) | 25 | 24 (18 + 6 external-gate) | 1 | 0 | 0 | PARTIAL (unit + MySQL) | NO (`PRODUCTION_CRYPTO_SUITE`, `PENDING_OWNER_DECISION`, device gates) |
| Addendum 002 — Platform Administration and Billing (`PCA-ADD-PA-*`, `PCA-ADD-BILL-*`) | 98 | 98 (96 + 2 external-gate) | 0 | 0 | 0 | PARTIAL (unit + MySQL incl. cross-realm, TOTP anti-replay, step-up CAS, 15 direct `WebhookService` MySQL tests covering freshness, redelivery, idempotency, `queryPayment` authority and amount/currency mismatch, and since 2026-09-08 invoice issuance on confirmed payment; no PA/BILL UAT) | NO (six commercial gates + `PLATFORM_ADMIN_ALERT_DELIVERY`) |
| Addendum 003 — Parent identity / registration / free access (`PCA-ADD-IDENT-*`) | 24 | 24 (23 + 1 external-gate) | 0 | 0 | 0 | PARTIAL (unit + MySQL; 0 of 4 `UAT-AUTH` cases) | NO (`PRODUCTION_EMAIL_DELIVERY`, `PRODUCTION_EMAIL_CREDENTIAL_ROTATION`) |
| Addendum 004 — Complimentary entitlements (`PCA-ADD-COMP-*`) | 25 | 25 | 0 | 0 | 0 | PARTIAL (unit + MySQL concurrency) | NO |

Base A-100 inventory (203 rows: the 199 originally counted plus the four owner-approved lettered additions `PCA-FR-043B`, `PCA-FR-043C`, `PCA-FR-015A`, `PCA-AND-003A`): 169 source-complete (151 + 16 external-gate + 2 validation-pending), 27 `PARTIAL`, 1 `NOT_STARTED`, 6 `NOT_APPLICABLE`.

**Traceability markdown is now derived, not hand-maintained (2026-09-08):** `docs/implementation/PCA_IMPLEMENTATION_TRACEABILITY.md`'s per-requirement status tables, status-distribution tables and completion counts are regenerated from the JSON matrix by `tooling/release/RegenerateTraceabilityTables.mjs`, and the release-control CI job runs its `--check`. The earlier R0/R4 counts it carried for weeks (e.g. Addendum 001: 4/9/11/1 while the JSON said 24/1/0) are gone; hand-written summaries survive with a dated "re-derived" prefix where their row's status had moved. The JSON matrix remains the machine-checked source of truth.

Addendum 002 workstream view (what the requirement counts do not show): `PCA-PA-1/2/3/4`, `PCA-BILL-1/2/3` and `PCA-MYKIDS-BILL-1` are `SOURCE_COMPLETE` (`PCA-ADD-PA-041`/`PCA-ADD-BILL-020` closed by commit `b439d94` and the settlement rollup; the earlier R4 notes were stale); `PCA-PA-5` (financial red team) is `PARTIAL` only for the human external review -- the webhook forgery/replay/freshness/idempotency scenarios are directly test-evidenced against MySQL; `PCA-PA-6` has real-browser contrast, forced-colours and reduced-motion evidence in CI and stays `PARTIAL` for screen-reader/device evidence; `PCA-PA-UAT` and `PCA-BILL-UAT` are `NOT_STARTED`. The production payment-provider registry is intentionally empty and triple-gated; no provider SDK exists in the tree.

## Programme control

- one main orchestrator;
- exact file/module ownership;
- no uncontrolled parallel writes to shared files;
- implementation agents must test their owned slice;
- independent red-team verification before phase acceptance;
- no "100% complete" report while any acceptance criterion is partial — and, per the three-tier framework above, no report may claim `VALIDATED_COMPLETE` or `PRODUCTION_READY` on the strength of `SOURCE_COMPLETE` alone;
- **this document is regenerated from executed evidence at every assessment pass, never edited to match a wish.** If it disagrees with `docs/supervision/PCA_FINAL_GAP_ASSESSMENT_2026-09-08.md` or a later assessment, the later dated assessment wins and this file is stale.
