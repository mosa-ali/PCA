# PCA synthetic privacy fixtures

`privacy-sentinels.json` contains invented, recognisable values for negative privacy tests. It is test data only and is deliberately the sole tracked location permitted to contain these exact markers. Do not replace any marker with family, child, credential, key, recovery, or production data.
